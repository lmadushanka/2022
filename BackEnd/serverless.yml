service: jpfruit
frameworkVersion: '3'

provider:
  name: aws
  runtime: nodejs14.x
  memorySize: 128
  timeout: 15
  stage: dev1
  region: us-east-1

functions:
  api:
    handler: lambda.handler
    events:
      - http: ANY /{proxy+}
      - http: ANY /
